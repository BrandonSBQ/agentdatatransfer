# Work Journal - 2026/06/17

## #1 - 15:21
**Commit:** `feat(health): add K8s liveness probe with test infrastructure`
**Files:** .gitignore, CMakeLists.txt, Dockerfile, src/main.cc, src/http/health_server.cc, src/http/health_server.h, tests/CMakeLists.txt, tests/port_holder.h, tests/health_server_test.cpp

Add a dedicated lightweight HTTP server (`HealthServer`) for the Kubernetes liveness probe on port 7070, runs in a background thread so the main API listener does not have to be responsive for the probe to succeed. Constructor throws `std::runtime_error` on bind failure so misconfiguration surfaces as `CrashLoopBackOff` instead of a delayed probe failure. Wire it into `main.cc` between config logging and the main listener, expose the port in the Dockerfile, default `HEALTH_ADDR=0.0.0.0:7070`. Add Catch2 v3 test infrastructure (`AGENT_BUILD_TESTS` option, `FetchContent` v3.5.4, `catch_discover_tests` for CTest) and 6 characterization tests covering type-level deletion, GET /healthy contract, routing isolation, bind-failure handling, destructor cleanup, and port/path independence — 6/6 pass, 0.20s. Enable `CMAKE_EXPORT_COMPILE_COMMANDS` and ignore the symlink so clangd resolves real include paths.

## #2 - 15:29
**Commit:** `feat(metadata): migrate from SQLite to MySQL with Aliyun OSS vendor`
**Files:** .gitignore, CMakeLists.txt, README.md, schema.sql, src/3rd/aliyun-oss-cpp-sdk, src/grpc/risk_rpc_client.cc, src/http/http_query_server.cc, src/http/http_query_server.h, src/http/risk_json_serializer.cc, src/main.cc, src/metadata/mysql_metadata_repository.cc, src/metadata/mysql_metadata_repository.h, src/metadata/sqlite_metadata_repository.cc, test.http

Conclude the c_dev merge of MySQL metadata migration: replace SQLite-backed `MetadataRepository` with a new `MysqlMetadataRepository` (1017 LoC, libmysqlclient via `mysql.h`) wired into `CMakeLists.txt`; port `schema.sql` to MySQL DDL (engine/charset/auto_increment). Vendor `aliyun-oss-cpp-sdk` under `src/3rd` as a tracked source dependency. Adjust HTTP query/JSON serializer, gRPC client, and `main.cc` to thread the new repository; refresh `test.http` and `README.md` for the new stack. `.gitignore` adds `Dockerfile.local`/`.dockerignore` host-only CI variant.
